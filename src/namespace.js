// Dependencies
// TODO: Remove unused dependencies and add new ones as needed.
var $assertion = $assertion || require('giant-assertion'),
    $commonWidgets = $commonWidgets || require('giant-common-widgets'),
    $data = $data || require('giant-data'),
    $entity = $entity || require('giant-entity'),
    $event = $event || require('giant-event'),
    $i18n = $i18n || require('giant-i18n'),
    $oop = $oop || require('giant-oop'),
    $routing = $routing || require('giant-routing'),
    $table = $table || require('giant-table'),
    $templating = $templating || require('giant-templating'),
    $transport = $transport || require('giant-transport'),
    $utils = $utils || require('giant-utils'),
    $widget = $widget || require('giant-widget'),
    jQuery = jQuery || require('jquery');

/**
 * TODO: Rename this to your namespace.
 * @namespace
 */
var $module = {};
