(function () {
    "use strict";

    // TODO: Change file and class name with your own.
    module("ModuleClass");

    // TODO: Change / replace tests with your own.
    test("Instantiation", function () {
        var instance = $module.ModuleClass.create();
        ok(instance, "should create new instance");
    });
}());
