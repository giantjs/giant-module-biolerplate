// TODO: Change file and class name to your own.
$oop.postpone($module, 'ModuleClass', function (ns, cn) {
    "use strict";

    var base = $oop.Base,
        self = base.extend();

    /**
     * @name $module.ModuleClass.create
     * @function
     * @returns {$module.ModuleClass}
     */

    /**
     * TODO: Implement class methods.
     * @class
     * @extends $oop.Base
     */
    $module.ModuleClass = self
        .addMethods(/** @lends $module.ModuleClass# */{
            /**
             * @ignore
             */
            init: function () {
            }
        });
});

(function () {
    "use strict";

    // TODO: Place global commands here. (Eg. assertion validator addition.)
}());
